'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-53b94a75.js');
const index$1 = require('./index-53f5a5fc.js');

const logoCss = ":host{--logo-size:36px}.logo{display:inline-flex;height:var(--logo-size);padding:calc(var(--logo-size) / 3);position:relative}.logo--variant-magenta{background-color:var(--scl-color-white)}.logo--variant-white{background-color:var(--scl-color-primary)}.logo--transparent{background-color:transparent}";

const Logo = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
    /** (optional) Variant/color of the logo text and logo */
    this.variant = 'magenta';
    /** (optional) Set transparent background */
    this.transparent = false;
    /** (optional) Language of the logo text/ claimOff showes just the T Logo */
    this.language = 'en';
    /** (optional) The height in pixels */
    this.size = 36;
    /** (optional) Set a link */
    this.href = '';
  }
  styles() {
    return `:host {
        --logo-size: ${this.size}px;
      }`;
  }
  getLogoSvg(role) {
    if (this.accessibilityTitle) {
      return (index.h("scale-logo-svg", { language: this.language, color: this.variant, size: this.size, accessibilityTitle: this.accessibilityTitle, role: role }));
    }
    else {
      return (index.h("scale-logo-svg", { language: this.language, color: this.variant, size: this.size, role: role }));
    }
  }
  render() {
    return (index.h(index.Host, null, index.h("style", null, this.styles()), this.href === '' ? (index.h("div", { class: this.getCssClassMap(), onClick: this.clickHandler }, this.getLogoSvg('img'))) : (index.h("a", { href: this.href, class: this.getCssClassMap(), onClick: this.clickHandler }, this.getLogoSvg('link')))));
  }
  getCssClassMap() {
    return index$1.classnames(`logo`, this.variant && `logo--variant-${this.variant}`, this.transparent && `logo--transparent`);
  }
  get hostElement() { return index.getElement(this); }
};
Logo.style = logoCss;

exports.scale_logo = Logo;
