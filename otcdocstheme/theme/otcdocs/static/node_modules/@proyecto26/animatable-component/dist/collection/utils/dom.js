/**
 * Get element to animate
 */
export const getElementToAnimate = (parent) => {
  return parent.shadowRoot !== null ? parent : (parent.firstElementChild
    || (parent.children.length && parent.children[0])
    || parent);
};
